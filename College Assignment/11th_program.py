#Create a Python function that counts and returns the number of words in a string. Consider
#spaces, punctuation, and other delimiters properly.

input_string = input("Enter a String: ")
result = input_string.count('')
print(result)